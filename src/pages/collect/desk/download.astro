---
import CollectDeskLayout from "@/layouts/CollectDeskLayout.astro";
import { url } from "@/utils/url-utils";
---
<CollectDeskLayout title="一键采集配置下载 · 影视采集站信号台" page="download">
	<header class="mast" id="hdr"></header>
	<div class="ticker" id="tick"></div>

	<div class="container prose" style="max-width:960px">
		<h2>苹果CMS一键采集接口配置文件下载</h2>
		<p id="lead" style="font-size:14.5px"></p>

		<div class="spec">
			<h3 style="font-family:var(--serif);font-size:20px;font-weight:900;margin-bottom:6px">配置文件说明</h3>
			<div class="spec-row"><div class="no">01</div><div class="t">筛选标准</div><p>先筛在线且可用率 ≥ 95%。下载时可再选：全部 / 常规（确认无 18+，不确定不要）/ 18+（明确成人）。</p></div>
			<div class="spec-row"><div class="no">02</div><div class="t">JSON 格式</div><p>结构化数据，包含采集站名称、API地址、接口类型、可用率、资源总量和分类列表，适合程序化批量导入。</p></div>
			<div class="spec-row"><div class="no">03</div><div class="t">TXT 格式</div><p>简洁的管道符分隔格式，每行一个采集站（名称|API地址|接口类型|可用率），适合手动复制粘贴到CMS后台。</p></div>
			<div class="spec-row"><div class="no">04</div><div class="t">兼容系统</div><p>苹果CMS V10、海洋CMS、飞飞CMS、赞片CMS 等主流影视CMS系统均可使用。</p></div>
		</div>

		<div class="dl-stats">
			<div class="sum"><div class="v" id="dlCount">—</div></div>
			<div class="sum"><div class="v" id="dlSafe">—</div></div>
			<div class="sum"><div class="v" id="dlAdult">—</div></div>
		</div>

		<div class="dl-btns">
			<button type="button" class="dl-btn" id="btnOpenDl">⇩ 选择并下载配置</button>
		</div>
		<div class="dl-meta" id="dlMeta"></div>

		<div class="dl-modal" id="dlModal" hidden>
			<div class="dl-modal-backdrop" data-close></div>
			<div class="dl-modal-card" role="dialog" aria-modal="true" aria-labelledby="dlModalTitle">
				<h3 id="dlModalTitle">选择下载范围</h3>
				<p class="dl-modal-tip">常规可含「伦理 / 港台三级」；一旦出现无码、有码、里番、情色等其它成人分类则进 18+。无分类的不进常规。请自行确认接口授权与内容合规。</p>
				<label class="dl-opt"><input type="radio" name="dlPack" value="all" /><span><b>下载全部高可用</b><small id="dlOptAll">—</small></span></label>
				<label class="dl-opt"><input type="radio" name="dlPack" value="safe" checked /><span><b>下载常规采集站</b><small id="dlOptSafe">—</small></span></label>
				<label class="dl-opt"><input type="radio" name="dlPack" value="adult" /><span><b>下载 18+ 采集站</b><small id="dlOptAdult">—</small></span></label>
				<div class="dl-modal-actions">
					<button type="button" class="dl-btn" id="btnJson">⇩ JSON</button>
					<button type="button" class="dl-btn alt" id="btnTxt">⇩ TXT</button>
				</div>
				<button type="button" class="dl-modal-cancel" data-close>取消</button>
			</div>
		</div>

		<h2>接口预览（前10个采集站）</h2>
		<table class="t">
			<thead><tr><th>采集站名称</th><th>接口类型</th><th>可用率</th><th>速度</th><th>资源量</th></tr></thead>
			<tbody id="dlPreview"></tbody>
		</table>
		<div class="dl-foot" id="dlMore" style="border:1px dashed var(--line);border-top:0;background:#fff6"></div>

		<h2>使用方法</h2>
		<div class="steps-box">
			<h4>苹果CMS V10 导入步骤</h4>
			<ol>
				<li>下载 JSON 或 TXT 格式配置文件</li>
				<li>登录苹果CMS后台，进入「采集」→「自定义采集」</li>
				<li>点击「批量导入」按钮，上传配置文件或粘贴内容</li>
				<li>系统自动识别并添加所有采集站接口</li>
				<li>进入「采集」→「一键采集」开始采集影视数据</li>
			</ol>
			<h4>海洋CMS 导入步骤</h4>
			<ol>
				<li>下载 TXT 格式配置文件</li>
				<li>登录海洋CMS后台，进入「资源库管理」→「添加资源库」</li>
				<li>逐条复制采集站信息填入对应字段，或通过数据库批量导入</li>
				<li>保存后即可开始采集</li>
			</ol>
		</div>

		<div class="about-box">
			<h4 style="font-family:var(--serif);font-size:18px;font-weight:900">关于一键采集接口配置</h4>
			<p>配置文件汇集本站每日巡检的优质接口。详细步骤可参考<a href={url("/collect/desk/guide/")}>采集对接教程</a>。</p>
		</div>
	</div>

	<footer class="site-foot" id="ftr"></footer>
</CollectDeskLayout>
